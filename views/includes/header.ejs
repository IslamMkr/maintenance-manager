<header>
    <h1>Gestionnaire de maintenance</h1>
    
    <% if (user) { %>
        <hr>
        <div class="user-bar">
            <h4><%= user.firstName %> <%= user.lastName %></h4>
            <form action="/disconnect" method="GET">
                <button class="btn-disconnect" type="submit">
                    Déconnexion
                </button>
            </form>
        </div>
    <% } %>
</header>